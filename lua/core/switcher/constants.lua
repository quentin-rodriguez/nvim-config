return {
  lazy_url = "https://github.com/folke/lazy.nvim.git",
  lazy_dir = vim.fn.stdpath("data") .. "/lazy/lazy.nvim",
  lazy_plugins_dir = vim.fn.stdpath("data") .. "/lazy",
  config_data_dir = vim.fn.stdpath("data") .. "/configs",
  configs_dir = vim.fn.stdpath("config") .. "/lua/configs",
  current_config_file = vim.fn.stdpath("cache") .. "/config.json"
 }
